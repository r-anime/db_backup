version: '3.9'
name: production

services:
  # only for dev / testing purposes
  db_backup:
    build: .
    command: ./db_backup -s stage_db -d r_anime_staging -b manual
    volumes:
      - ./ztest/srv:/srv
      - /var/run/docker.sock:/var/run/docker.sock
